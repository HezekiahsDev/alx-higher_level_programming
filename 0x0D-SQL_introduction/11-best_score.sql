-- lists all rows of the table
-- from the database display scores and name
-- ordered by score in descemding order from score >= 10
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
